"use client";

import StagePage from "@/components/StagePage";

export default function QualityLeadsPage() {
  return (
    <StagePage
      stage="Quality Leads"
      title="Quality Leads"
      description="A real structured back-and-forth — automatic message, our reply, lead replied, we replied, and the lead asked or said something else again."
    />
  );
}
