"use client";

import StagePage from "@/components/StagePage";

export default function InterestedPage() {
  return (
    <StagePage
      stage="Interested"
      title="Interested"
      description="Conversation has happened multiple times — sustained back-and-forth across many exchanges."
    />
  );
}
