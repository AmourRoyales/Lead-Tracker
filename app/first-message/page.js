"use client";

import StagePage from "@/components/StagePage";

export default function FirstMessagePage() {
  return (
    <StagePage
      stage="First Message"
      title="First Message"
      description="Only an initial automated/template message has gone out (with at most one reply layered on top) — low-quality leads that haven't shown real engagement yet."
    />
  );
}
